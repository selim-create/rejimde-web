# Inbox & Messaging - Architecture Diagram

## System Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                         Frontend (Next.js)                       │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ┌────────────────────┐         ┌────────────────────┐         │
│  │   Inbox List Page  │────────▶│  Thread Detail     │         │
│  │  /pro/inbox        │         │  /pro/inbox/[id]   │         │
│  └────────────────────┘         └────────────────────┘         │
│           │                               │                     │
│           │                               │                     │
│           ▼                               ▼                     │
│  ┌─────────────────────────────────────────────────┐          │
│  │         Reusable Components Layer                │          │
│  ├─────────────────────────────────────────────────┤          │
│  │ • ThreadCard      • MessageBubble               │          │
│  │ • ThreadList      • MessageInput                │          │
│  │ • NewThreadModal  • TemplateSelector            │          │
│  └─────────────────────────────────────────────────┘          │
│                           │                                     │
│                           ▼                                     │
│  ┌─────────────────────────────────────────────────┐          │
│  │          API Functions Layer (lib/api.ts)        │          │
│  ├─────────────────────────────────────────────────┤          │
│  │ • getInboxThreads    • sendInboxMessage         │          │
│  │ • getInboxThread     • createInboxThread        │          │
│  │ • markThreadAsRead   • closeInboxThread         │          │
│  │ • getTemplates       • generateAIDraft          │          │
│  └─────────────────────────────────────────────────┘          │
│                           │                                     │
└───────────────────────────┼─────────────────────────────────────┘
                            │
                            ▼ HTTP Requests
┌─────────────────────────────────────────────────────────────────┐
│                    Backend API (WordPress)                       │
├─────────────────────────────────────────────────────────────────┤
│  Base URL: /rejimde/v1/pro/                                     │
│                                                                  │
│  Endpoints:                                                      │
│  • GET    /inbox                    - List threads              │
│  • GET    /inbox/{id}               - Get thread & messages     │
│  • POST   /inbox/threads            - Create thread             │
│  • POST   /inbox/{id}/messages      - Send message              │
│  • POST   /inbox/{id}/read          - Mark as read              │
│  • POST   /inbox/{id}/close         - Close thread              │
│  • POST   /inbox/{id}/archive       - Archive thread            │
│  • GET    /message-templates        - Get templates             │
│  • POST   /message-templates        - Create template           │
│  • DELETE /message-templates/{id}   - Delete template           │
│  • POST   /inbox/{id}/ai-draft      - Generate AI draft         │
│  • GET    /inbox/unread-count       - Get unread count          │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│                         Database                                 │
├─────────────────────────────────────────────────────────────────┤
│  Tables:                                                         │
│  • inbox_threads        - Thread metadata                       │
│  • inbox_messages       - Messages                              │
│  • message_templates    - Saved templates                       │
│  • relationships        - Expert-client relationships            │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

## Data Flow - Sending a Message

```
┌──────────────┐
│ User Types   │
│ Message      │
└──────┬───────┘
       │
       ▼
┌──────────────────────────┐
│ MessageInput Component   │
│ - Captures input         │
│ - Validates content      │
└──────┬───────────────────┘
       │ onSend()
       ▼
┌──────────────────────────┐
│ Thread Detail Page       │
│ - Optimistic UI update   │
│ - Show message instantly │
└──────┬───────────────────┘
       │ handleSendMessage()
       ▼
┌──────────────────────────┐
│ sendInboxMessage()       │
│ (lib/api.ts)             │
└──────┬───────────────────┘
       │ POST request
       ▼
┌──────────────────────────┐
│ Backend API              │
│ /inbox/{id}/messages     │
│ - Saves to database      │
│ - Returns message object │
└──────┬───────────────────┘
       │ Response
       ▼
┌──────────────────────────┐
│ Thread Detail Page       │
│ - Updates with real data │
│ - Shows success toast    │
└──────────────────────────┘
```

## Component Tree

```
App
└── Dashboard Layout
    └── Pro Layout
        └── Inbox Page (/pro/inbox)
            ├── Header
            │   ├── PRO PANEL badge
            │   ├── Title: "Gelen Kutusu"
            │   └── Avatar
            │
            ├── Sidebar (Desktop)
            │   ├── Panel link
            │   ├── Notifications link
            │   ├── Inbox link (active)
            │   ├── Clients link
            │   └── ... other links
            │
            └── Inbox Content
                ├── Filter Tabs
                │   ├── All
                │   ├── Open
                │   ├── Closed
                │   └── Archived
                │
                ├── Search Input
                ├── New Message Button
                │
                └── ThreadList
                    ├── ThreadCard (thread 1)
                    ├── ThreadCard (thread 2)
                    └── ThreadCard (thread n)
                    
            [When clicking a thread, navigate to:]
            
        Thread Detail Page (/pro/inbox/[threadId])
            ├── Header
            │   ├── Back button
            │   ├── Thread subject
            │   └── Avatar
            │
            └── Thread Content
                ├── Thread Header
                │   ├── Client info
                │   ├── Status badge
                │   ├── Close button
                │   └── Archive button
                │
                ├── Messages Area
                │   ├── MessageBubble (client)
                │   ├── MessageBubble (expert)
                │   └── ... more messages
                │
                └── MessageInput
                    ├── Quick Actions
                    │   ├── AI Draft button
                    │   └── Template button
                    │
                    ├── TemplateSelector (if shown)
                    │   └── Template list by category
                    │
                    └── Input Area
                        ├── Textarea
                        └── Send button
```

## State Management Flow

```
┌─────────────────────────────────────────────────────────────┐
│                    Inbox List Page State                     │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  threads: InboxThread[]         ◄──── getInboxThreads()    │
│  meta: { total, unread_total }  ◄──── from API             │
│  loading: boolean                                           │
│  activeTab: 'all' | 'open' ...                             │
│  searchTerm: string                                         │
│                                                              │
│  User Actions:                                              │
│  • Change tab    ──────▶ Re-fetch with filter              │
│  • Type search   ──────▶ Debounce ──────▶ Re-fetch         │
│  • Click thread  ──────▶ Navigate to detail                │
│  • New message   ──────▶ Show modal ──────▶ Create thread  │
│                                                              │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│                  Thread Detail Page State                    │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  thread: InboxThread          ◄──── getInboxThread()       │
│  messages: InboxMessage[]     ◄──── from API               │
│  templates: MessageTemplate[] ◄──── getMessageTemplates()  │
│  loading: boolean                                           │
│  sending: boolean                                           │
│                                                              │
│  User Actions:                                              │
│  • Type message  ──────▶ Update local state                │
│  • Send message  ──────▶ Optimistic UI ──────▶ API call    │
│  • AI draft      ──────▶ Call API ──────▶ Send message     │
│  • Select tmpl   ──────▶ Insert into input                 │
│  • Close thread  ──────▶ API call ──────▶ Update status    │
│  • Archive       ──────▶ API call ──────▶ Redirect to list │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

## UI States Diagram

```
┌────────────────────────────────────────────────────────────┐
│                     Inbox List States                       │
├────────────────────────────────────────────────────────────┤
│                                                             │
│  Loading:                    Empty:                        │
│  ┌──────────────┐           ┌──────────────┐             │
│  │ ╔══════════╗ │           │      📁       │             │
│  │ ║▓▓▓▓▓▓▓▓▓▓║ │           │   Mesaj yok   │             │
│  │ ║▓▓▓▓▓▓▓▓▓▓║ │           └──────────────┘             │
│  │ ╚══════════╝ │                                         │
│  └──────────────┘           Loaded with data:             │
│                             ┌──────────────┐              │
│  Error:                     │ 👤 Name      │              │
│  ┌──────────────┐           │ ● Subject    │              │
│  │      ⚠️       │           │ Preview...   │              │
│  │ Hata oluştu  │           ├──────────────┤              │
│  │ [Tekrar dene]│           │ 👤 Name      │              │
│  └──────────────┘           │ Subject      │              │
│                             │ Preview...   │              │
│                             └──────────────┘              │
└────────────────────────────────────────────────────────────┘

┌────────────────────────────────────────────────────────────┐
│                   Thread Detail States                      │
├────────────────────────────────────────────────────────────┤
│                                                             │
│  Loading:                    Empty Messages:               │
│  ┌──────────────┐           ┌──────────────┐             │
│  │      ⏳       │           │      💬       │             │
│  │ Yükleniyor.. │           │ Mesaj yok     │             │
│  └──────────────┘           └──────────────┘             │
│                                                             │
│  With Messages:             Sending:                       │
│  ┌──────────────┐           ┌──────────────┐             │
│  │ Client msg   │           │ Expert msg   │ (opacity)    │
│  │ Expert msg   │           │ ⏳ Sending..  │             │
│  │ Client msg   │           └──────────────┘             │
│  └──────────────┘                                         │
│                             Thread Closed:                 │
│  Error Sending:             ┌──────────────┐             │
│  ┌──────────────┐           │      🔒       │             │
│  │ Expert msg X │           │ Kapatılmış    │             │
│  │ (Failed)     │           └──────────────┘             │
│  └──────────────┘                                         │
└────────────────────────────────────────────────────────────┘
```

## Responsive Behavior

```
Mobile (< 768px):
┌─────────────┐
│   Header    │
├─────────────┤
│             │
│  Thread     │
│  List       │
│  (Full)     │
│             │
└─────────────┘
     │ click thread
     ▼
┌─────────────┐
│ ← Header    │
├─────────────┤
│             │
│  Messages   │
│  (Full)     │
│             │
├─────────────┤
│ Input Area  │
└─────────────┘

Tablet/Desktop (>= 768px):
┌────────────────────────────────┐
│          Header                 │
├────┬───────────────────────────┤
│Side│  Thread List | Messages   │
│bar │              |             │
│    │              |             │
│    │              |             │
│    │              |─────────────│
│    │              | Input Area  │
└────┴──────────────┴─────────────┘
```

## Error Handling Flow

```
API Call Failed
      │
      ▼
┌─────────────────┐
│ Catch Error     │
└─────┬───────────┘
      │
      ├──▶ Toast Notification (Error type)
      │    "Mesaj gönderilemedi"
      │
      ├──▶ Rollback Optimistic UI
      │    (Remove temp message)
      │
      └──▶ Log to Console
           (For debugging)

Network Error
      │
      ▼
┌─────────────────┐
│ Show Empty State│
│ or Retry Button │
└─────────────────┘
```

This architecture provides a clear separation of concerns, efficient data flow, and a great user experience!
